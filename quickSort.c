#include <stdio.h>
#include <stdlib.h>

//This function is for swapping two numbers

void swap(int *a, int *b)
{
	int temp;
	temp = *a;
	*a = *b;
	*b = temp;
}

int partition(int A[], int si, int ei)
{
	
}